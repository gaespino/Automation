import './style.css';

export default function FileHandler() {
  return (
    <div className="tool-page">
      <h2 className="page-title">FileHandler</h2>
      <p className="dim">Full REST implementation — API at <code>/api/filehandler</code></p>
    </div>
  );
}
