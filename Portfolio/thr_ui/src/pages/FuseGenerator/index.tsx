import './style.css';

export default function FuseGenerator() {
  return (
    <div className="tool-page">
      <h2 className="page-title">FuseGenerator</h2>
      <p className="dim">Full REST implementation — API at <code>/api/fusegenerator</code></p>
    </div>
  );
}
