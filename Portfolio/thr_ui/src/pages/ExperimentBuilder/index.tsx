import './style.css';

export default function ExperimentBuilder() {
  return (
    <div className="tool-page">
      <h2 className="page-title">ExperimentBuilder</h2>
      <p className="dim">Full REST implementation — API at <code>/api/experimentbuilder</code></p>
    </div>
  );
}
