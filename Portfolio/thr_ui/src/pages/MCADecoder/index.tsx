import './style.css';

export default function MCADecoder() {
  return (
    <div className="tool-page">
      <h2 className="page-title">MCADecoder</h2>
      <p className="dim">Full REST implementation — API at <code>/api/mcadecoder</code></p>
    </div>
  );
}
