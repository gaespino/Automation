# Appendix: Config File Reference

## ControlPanelConfig format
`{product}ControlPanelConfig.json`:

`json
{
  "enabled_fields": ["name", "vid", "ww", "bucket"],
  "default_values": { "environment": "SLT" }
}
`"

## Data JSON format
See `data/GNR/` for example unit JSON structures used by the Dashboard.
