# Portfolio Unified App — Requirements
# Install: pip install -r requirements.txt
# Generated: 2026-02-26

# --- Web Framework ---
dash==3.3.0
dash-ag-grid==32.3.2
dash-bootstrap-components==2.0.4

# --- REST API (THR Tools) ---
fastapi>=0.111.0
uvicorn[standard]>=0.29.0
python-multipart>=0.0.9
starlette>=0.37.0

# --- Data Processing ---
pandas>=1.3.0
numpy>=1.24.0
openpyxl>=3.0.0

# --- Production Server (CaaS) ---
# Dash dashboard:   gunicorn app:server
# THR Tools REST:   uvicorn api.main:app --host 0.0.0.0 --port 8000
gunicorn>=21.2.0

# --- Formatting / Output ---
colorama>=0.4.4
tabulate>=0.8.9

# --- Testing ---
pytest>=7.0.0
pytest-mock>=3.0.0
httpx>=0.27.0          # FastAPI TestClient dependency

# --- Optional: Excel advanced features (PPV tools) ---
# xlwings requires Excel installed — not suitable for CaaS headless environment
# xlwings>=0.24.0   # commented out; use openpyxl for read/write instead
dash-cytoscape>=1.0.0
