; Commands.ttl
; Set up the serial port connection
; COM Port to be used taken from Env Variable

; Open the serial port
; Below an example of how to manually set this up
; connect '/C=11'
; Wil use an env variable to set the port -- configuration is being taken care at .ini file

; Get Framework SerialHost
getenv 'FrameworkSerial' SerialHost
connect '/C=SerialHost'

timeout = 10
logopen 'C:\Temp\capture.log'

; Wait for the connection to establish
sendln '#CONNECT'
pause 5

; Remove the wait in case we are going straight for linux --
wait 'CONNECT'
if result=0 goto ok
if result=1 goto ok
if result=2 goto error

:error
end

:ok

;Call your Macro
; Macro for Linux OS Contents
include 'LinuxContent.ttl'

end